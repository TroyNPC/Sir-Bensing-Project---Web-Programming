<div class="sidebar d-flex flex-column">
    <div class="logo">
        <img src="{{ asset('Icon.png') }}" alt="Logo" 
             style="width: 50px; height: 40px; filter: grayscale(1) brightness(0) invert(1);">
        <h4>NexusCore</h4>
    </div>

    <ul class="nav flex-column">
        <!-- Dashboard -->
        <li class="nav-item">
            <a href="{{ path('app_admindashboard') }}" 
               class="nav-link {% if app.request.get('_route') == 'app_admindashboard' %}active{% endif %}">
                <i class="bi bi-speedometer2 me-2"></i> Dashboard
            </a>
        </li>

        <!-- Products -->
        <li class="nav-item">
            <a href="{{ path('app_pcproducts_index') }}" 
               class="nav-link {% if 'pcproducts' in app.request.get('_route') %}active{% endif %}">
                <i class="bi bi-box-seam me-2"></i> Products List
            </a>
        </li>

        <!-- Add Product -->
        <li class="nav-item">
            <a href="{{ path('app_pcproducts_new') }}" 
               class="nav-link {% if app.request.get('_route') == 'app_pcproducts_new' %}active{% endif %}">
                <i class="bi bi-plus-circle me-2"></i> Add Product
            </a>
        </li>

        <!-- Users -->
        <li class="nav-item">
            <a href="{{ path('app_user_index') }}" 
               class="nav-link {% if 'user' in app.request.get('_route') %}active{% endif %}">
                <i class="bi bi-people me-2"></i> Users List
            </a>
        </li>

        <!-- Add User -->
        <li class="nav-item">
            <a href="{{ path('app_user_new') }}" 
               class="nav-link {% if app.request.get('_route') == 'app_user_new' %}active{% endif %}">
                <i class="bi bi-person-plus me-2"></i> Add User
            </a>
        </li>

        <!-- Service Booking -->
        <li class="nav-item">
            <a href="{{ path('app_servicebooking_index') }}" 
               class="nav-link {% if app.request.get('_route') == 'app_servicebooking_index' %}active{% endif %}">
                <i class="bi bi-calendar-check me-2"></i> Service Booking
            </a>
        </li>
    </ul>
</div>

<style>
:root {
    --dark-bg: #2E2E2E;
    --accent: #F88F6A;
    --light-bg: #f5f5f5;
}

.sidebar {
    position: fixed;
    top: 0;
    left: 0;
    width: 240px;
    height: 100vh;
    background-color: var(--dark-bg);
    color: white;
    padding: 1rem;
}

.sidebar .logo {
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
}

.sidebar h4 {
    font-family: 'Montserrat', sans-serif;
    margin-left: 10px;
}

.sidebar .nav-link {
    color: #ccc;
    text-decoration: none;
    padding: 0.75rem 1.25rem;
    border-radius: 8px;
    transition: all 0.2s ease;
}

.sidebar .nav-link:hover {
    background-color: rgba(248, 143, 106, 0.15);
    color: #fff;
}

.sidebar .nav-link.active {
    background-color: var(--accent);
    color: #2E2E2E;
    font-weight: 600;
}
</style>
